# 🔵 1. Type vs Interface in TypeScript

TypeScript শেখার সময়ে আমরা দুইটি প্রধান জিনিস দেখি:

- **type**
- **interface**

দুটোই অবজেক্টের টাইপ বানাতে পারে, কিন্তু পার্থক্য আছে

---

## ১. Interface কী?

Interface = অবজেক্টের blueprint
যে property থাকবে এবং টাইপ কী হবে, সব define করে

`interface  User { name: string; age: number;
}`

**সুবিধা :**

- clean এবং readable
- extend করা সহজ
- declaration merging সমর্থন করে
- class, লাইব্রেরি, API-তে ব্যবহারযোগ্য

---

## ২. Type Alias কী?

Type = nickname বা alias
object, union, tuple, function, primitive সব define করা যায়

`type  User = { name: string; age: number };`
`type  ID = string | number; `
`type  Point = [number, number];`

**সুবিধা :**

- union, intersection, tuple, function, primitive alias
- advanced type logic তৈরি করা যায়

Interface এত flexible নয়।

---

## ৩. Declaration Merging

Interface একাধিকবার declare করলে merge হয়:

`interface  User { name: string } `
`interface  User { age: number }`

Result → `{ name: string; age: number }`

Type Alias-এ merging হয় না

---

## ৪. Extending

`interface A { a: number }`
` interface B extends A { b: number }`
` type  A2 = { a: number }`
` interface B2 extends A2 { b: number }`
`interface A3 { a: number } `
`type  B3 = A3 & { b: number }`

Interface natural এবং বেশি readable

---

## ৫. Function, Tuple, Union

- Function: type & interface দুইটিতেই possible
- Tuple, union, primitive: শুধুমাত্র type দিয়ে possible

`type  Add = (x:number,y:number)=>number;`
` type  Status = "success"|"error";`
` type  Point = [number,number];`

---

## 🎯 Conclusion — তাহলে কখন কোনটা ব্যবহার করবেন?

আধুনিক TypeScript কমিউনিটির একদম সরল guideline হলো—

### ✔ অবজেক্ট শেইপ → **interface**

### ✔ বাকি সব advanced টাইপ → **type**

সংক্ষেপে:

- **interface** → clean, organized, expandable
- **type** → powerful, flexible, universal

---

# 🔵 2. TypeScript-এর তিন রহস্য: any, unknown এবং never — পার্থক্য ও ব্যবহার

TypeScript শিখতে গিয়ে আমরা `string`, `number`, `boolean` নিয়ে খুব সুখে শান্তিতেই থাকি। কিন্তু হঠাৎ করেই সামনে এসে দাঁড়ায় তিনটি অদ্ভুত ক্যারেক্টার:

1.  **`any`**
2.  **`unknown`**
3.  **`never`**

প্রথম দেখায় মনে হতে পারে—"ভাই, এগুলা কি খায় না মাথায় দেয়?" 🤯

বিশেষ করে `any` আর `unknown` তো যমজ ভাইয়ের মতো মনে হয়। আর `never` আবার কেমন টাইপ যেটা কখনও ঘটেই না?

চলুন, কোনো কঠিন সংজ্ঞা ছাড়াই একদম গল্পের ছলে এই তিনটির পোস্টমর্টেম করি! 🕵️‍♂️

## ১. `any` — "আমি যা খুশি তাই করব!" 😎

`any` হলো TypeScript-এর **"ব্যাড বয়"**। একে ব্যবহার করা মানে হলো TypeScript-কে বলা— _"ভাই, তুমি চুপ থাকো, আমি কী করছি আমি জানি ।"_

যখন আপনি কোনো ভেরিয়েবলের টাইপ `any` দেন, তখন TypeScript তার টাইপ চেকিং **বন্ধ** করে দেয়। এটা জাভাস্ক্রিপ্টের মতো আচরণ করে।

### কখন ব্যবহার করবেন?

সত্যি বলতে? **যতটা সম্ভব কম!** তবে—

- খুব পুরনো কোনো JavaScript প্রজেক্ট TypeScript-এ কনভার্ট করছেন
- খুবই জটিল কোনো থার্ড-পার্টি লাইব্রেরি ব্যবহার করছেন যার টাইপ আপনি জানেন না
- কুইক প্রোটোটাইপিং বা হ্যাকathon-এ যখন টাইপ ফিক্স করার সময় নেই

### সমস্যা কোথায়?

`any` ব্যবহার করলে TypeScript-এর আসল মজাই নষ্ট হয়ে যায়। ভুলভাল কোড লিখলেও কম্পাইলার কিছু বলবে না, কিন্তু রানটাইমে অ্যাপ ক্র্যাশ করবে! 💥

```
let randomValue: any = 10;

randomValue = "Hello";  // সমস্যা নেই
randomValue = true;     // সমস্যা নেই

// ❌ বিপদ এখানে!
// boolean-এর তো কোনো func() মেথড নেই, কিন্তু TS আটকাবে না
// অ্যাপ রান করলে ধপাস করে ক্র্যাশ করবে!
randomValue.func();

```

## ২. `unknown` — "আগে জানব, তারপর মানব" 🤔

`unknown` হলো `any`-র ভদ্র এবং নিরাপদ সংস্করণ (Safe Counterpart)।

এটিও যেকোনো ভ্যালু নিতে পারে। **কিন্তু পার্থক্য হলো—** আপনি চাইলেই একে যা খুশি তাই করতে পারবেন না। TypeScript বলবে, _"ভাই, আগে চেক করো এটা আসলে কী টাইপ, তারপর ব্যবহার করো।"_

একে বলা যায় **"Locked Box"**। আপনি বক্সের ভেতরে যেকোনো কিছু রাখতে পারেন, কিন্তু বক্স না খুলে ভেতরে কী আছে তা ব্যবহার করতে পারবেন না।

### কোড দেখলে ক্লিয়ার হবে:

```
let secretValue: unknown = "Hello World";

// ❌ TypeScript এরর দিবে!
// কারণ TS জানে না secretValue আসলে স্ট্রিং নাকি অন্য কিছু।
// console.log(secretValue.toUpperCase());

// ✅ সঠিক উপায় (Type Guard)
if (typeof secretValue === "string") {
  // এখন TS নিশ্চিত যে এটা স্ট্রিং
  console.log(secretValue.toUpperCase()); // আউটপুট: HELLO WORLD
}

```

### কখন ব্যবহার করবেন?

- যখন API থেকে ডাটা আসছে কিন্তু আপনি টাইপ সম্পর্কে নিশ্চিত নন।
- `any`-র পরিবর্তে সব সময় `unknown` ব্যবহার করা বেস্ট প্র্যাকটিস।

## ৩. `never` — "ফিরে না আসার গল্প" 🚫

`never` টাইপটা একটু ফিলোসফিক্যাল। এর মানে হলো— **"এমন কিছু যা কখনোই ঘটবে না"** বা **"যেখান থেকে কোড আর ফিরে আসে না"**।

এটা সাধারণত এমন ফাংশনে ব্যবহার হয় যা:

1.  সব সময় এরর (Error) থ্রো করে।
2.  অথবা, ইনফিনিট লুপে (Infinite Loop) চলে।

অর্থাৎ, ফাংশনটা তার শেষ লাইনে কখনোই পৌঁছাতে পারে না।

### উদাহরণ:

```
// ১. এরর থ্রো করা
function throwError(message: string): never {
  throw new Error(message);
  // এই ফাংশন এক্সিকিউশন শেষ করে কখনোই রিটার্ন করবে না।
}

// ২. ইনফিনিিট লুপ
function infiniteLoop(): never {
  while (true) {
    console.log("চলতেই থাকবে...");
  }
}

```

## 🎯 Conclusion

TypeScript-এ ভালো হতে চাইলে:

1.  `any` কে **শত্রু** ভাবুন। (খুব ঠেকায় না পড়লে ব্যবহার করবেন না)
2.  অজানা ডাটার জন্য `any`-র বদলে **`unknown`** ব্যবহার করার অভ্যাস করুন
3.  কোড লজিকের ফাঁকফোকর ধরতে **`never`** ব্যবহার করুন (বিশেষ করে `switch-case` এরর হ্যান্ডলিংয়ে)।

আশা করি এই তিন মূর্তির রহস্য এখন পরিষ্কার! হ্যাপি কোডিং! 🚀💻


